Ignora todas as pastas de dependências

node_modules
**/node_modules
dist
frontend/.next
frontend/.vercel
.vscode
.git
.env
.env.local
.env.*
*.log


Ignora metadados e arquivos de sistema

.gitignore
.DS_Store
.idea
npm-debug.log*
yarn-debug.log*
yarn-error.log*

Ignora artefatos de build que serão criados DENTRO do container

O NestJS e React geram estes arquivos

build
coverage

Ignora arquivos de ambiente local (devem ser passados como ARG/ENV)



Opcional: Ignorar a pasta do serviço "irmão"

No seu caso, o context já restringe isso, mas é uma boa prática

se você não precisar dos arquivos irmãos para a compilação.

Ex: ao compilar o backend, não precisamos dos arquivos do frontend.

frontend/